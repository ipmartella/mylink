#include <markdown_storage.h>

#include <string>

using namespace mylink;

namespace {

bool is_white_space_only(const std::string& line) {}

} //namespace

Bookmark parse_markdown_line(const std::string& line) {}
